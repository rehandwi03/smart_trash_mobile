import 'package:flutter/material.dart';

class CardData {
  IconData icon;
  Color iconColor;
  String cardContent;
  String footerText;
  String number;
  String route;

  CardData(this.icon, this.iconColor, this.cardContent, this.footerText,
      this.number, this.route);
}
